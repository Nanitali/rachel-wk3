<div class="container">
  <div class="oponent">
    <img src="{{thing1.url}}">
    <h3>{{thing1.name}}</h3>
  </div>
  <div class="vs">
    <h3>vs.</h3>
  </div>
   <div class="oponent">
    <img src="{{thing2.url}}">
    <h3>{{thing2.name}}</h3>
  </div>
</div>
<div class="form-container">
  <form action="/random" method="POST">
    <input type="submit" value="try another one">
  </form>
</div>
